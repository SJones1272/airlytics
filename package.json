{
  "name": "api",
  "version": "1.0.0",
  "description": "API for airlytics",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Stephen Jones",
  "license": "MIT",
  "dependencies": {
    "elasticsearch": "^15.4.1",
    "express": "^4.16.4"
  },
  "engines": {
    "node": "11.10.0"
  }
}
